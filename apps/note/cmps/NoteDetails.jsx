export const NoteDetails = ({ isShown, noteToEdit, onClose }) => {
  console.log(noteToEdit)

  // ;<section className="survey">
  //   <h2>Survey - {survey.title}</h2>

  //   {survey.cmps.map((cmp) => (
  //     <DynamicCmp
  //       key={cmp.id}
  //       cmpType={cmp.type}
  //       info={cmp.info}
  //       val={answersMap[cmp.id] || ''}
  //       onChangeVal={(val) => handleChange(cmp.id, val)}
  //     />
  //   ))}

  //   <hr />
  //   <pre>{JSON.stringify(answersMap, null, 2)}</pre>
  // </section>

  return <div>Note Details</div>
}
